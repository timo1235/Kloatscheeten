services:
  kloatscheeten:
    build: .
    container_name: kloatscheeten
    restart: unless-stopped
    ports:
      - "12000:12000"
    volumes:
      - data:/app/data
    environment:
      - PORT=12000

volumes:
  data:
